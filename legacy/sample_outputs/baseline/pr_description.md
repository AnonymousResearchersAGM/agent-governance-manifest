# Baseline PR Description

This PR updates authentication behavior and adds a regression test.

Tests were run locally and passed.

Notes:
- Changed `app/auth.py`.
- Updated `tests/test_auth.py`.

This baseline example intentionally does not include the complete AGM evidence package, risk-zone declaration, trace manifest, or human review declaration.

