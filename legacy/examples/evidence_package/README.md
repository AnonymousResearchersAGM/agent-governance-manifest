# Evidence Package Examples

These folders are illustrative snapshots copied from `sample_outputs/manifest_supported/`.

- `completed_critical_auth_change/`: completed evidence package that should pass validation.
- `skeleton_critical_auth_change/`: intentionally incomplete skeleton package that should fail validation until placeholders are replaced.

The canonical sample outputs remain under `../../sample_outputs/manifest_supported/`.

