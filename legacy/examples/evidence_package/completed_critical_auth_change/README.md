# Completed Critical Auth Change Evidence

This snapshot mirrors the completed `critical_auth_change` evidence package. It is intended for quick public artifact inspection. Use the canonical sample package under `sample_outputs/manifest_supported/critical_auth_change/` for reproduction commands.

